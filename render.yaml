services:
  - type: web
    name: onder-papirbolt
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        name: X-Robots-Tag
        value: all
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
